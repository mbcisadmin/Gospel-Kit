# ==========================================
# MinistryPlatform API Configuration
# ==========================================
# Your MinistryPlatform instance base URL (without trailing slash)
# Example: https://my.yourchurch.org/ministryplatformapi
MINISTRY_PLATFORM_BASE_URL=https://your-church.ministryplatform.com/ministryplatformapi

# OAuth Client credentials from MinistryPlatform
# Get these from: Admin > API Clients in MinistryPlatform
# Create a new API Client with "oauth" grant type
MINISTRY_PLATFORM_CLIENT_ID=your-client-id
MINISTRY_PLATFORM_CLIENT_SECRET=your-client-secret

# Public file URL for MinistryPlatform (used for images, documents, etc.)
# Format: {base-url}/files
NEXT_PUBLIC_MINISTRY_PLATFORM_BASE_URL=https://your-church.ministryplatform.com/ministryplatformapi
NEXT_PUBLIC_MINISTRY_PLATFORM_FILE_URL=https://your-church.ministryplatform.com/ministryplatformapi/files

# ==========================================
# NextAuth Configuration
# ==========================================
# Secret key for encrypting session tokens
# REQUIRED: Generate a secure random string with:
#   openssl rand -base64 32
# IMPORTANT: Use different secrets for dev/staging/production
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# The base URL of your application
# Development: http://localhost:3000
# Production: https://your-app.vercel.app or your custom domain
NEXTAUTH_URL=http://localhost:3000

# ==========================================
# Permissions Configuration
# ==========================================
# The Security Role ID that grants admin access
# Find this in MinistryPlatform: Admin > Security Roles > [Your Admin Role] > check URL for Role_ID
# Users with this role will have session.isAdmin = true
ADMIN_SECURITY_ROLE_ID=

# ==========================================
# Neon PostgreSQL Database (Optional)
# ==========================================
# Connection string for Neon serverless PostgreSQL
# Get this from: Neon Console > Connection Details > Connection String
# Only required if using Neon for app permissions and custom data
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require

# ==========================================
# Central Database (Multi-Tenant)
# ==========================================
# Connection string for the central Neon database that holds tenant metadata.
# Only required when running in multi-tenant mode (*.churchhub.dev subdomains).
# Single-tenant deployments (one church per Vercel project) do not need this.
CENTRAL_DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/central?sslmode=require

# ==========================================
# Church Branding (NOT in .env)
# ==========================================
# Church name, tagline, app name, and other identity values are configured
# directly in: apps/platform/src/config/church.ts
# Brand colors are configured in: apps/platform/src/app/globals.css

# ==========================================
# Feature Flags
# ==========================================
# Set to 'true' to use mock data for features that need MinistryPlatform stored procedures
# Useful for template demos and development without an MP instance
USE_MOCK_DATA=false

# ==========================================
# Optional: Development Tools
# ==========================================
# Node environment (usually set automatically by hosting platform)
NODE_ENV=development

# ==========================================
# Optional: Additional Integrations
# ==========================================
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-api-key
